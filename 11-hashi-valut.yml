- name: Retrieve vault secrets
  hosts: localhost
  connection: local
  tasks:
    - name: Get secrets KV
      ansible.builtin.debug:
      msg: "{{ lookup('community.hashi_vault.vault_kv2_get', 'demo', url='https://vault:8200') }}"
